default_install_hook_types: [pre-commit, pre-push]
default_stages: [pre-commit]

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      # Vendored third-party assets are excluded from the file-content hooks:
      # they must stay byte-identical to what upstream publishes, because
      # tests/test_docs_page.py verifies them against recorded SHA-256 digests.
      # A hook that rewrites them would silently invalidate that check.
      - id: check-added-large-files
        args: [--maxkb=1000]
        stages: [pre-commit]
        exclude: ^app/static/vendor/
      - id: check-case-conflict
        stages: [pre-commit]
      - id: check-json
        stages: [pre-commit]
      - id: check-merge-conflict
        stages: [pre-commit]
      - id: detect-private-key
        stages: [pre-commit]
      - id: check-toml
        stages: [pre-commit]
      - id: check-yaml
        stages: [pre-commit]
      - id: end-of-file-fixer
        stages: [pre-commit]
        exclude: ^app/static/vendor/
      - id: trailing-whitespace
        stages: [pre-commit]
        exclude: ^app/static/vendor/

  - repo: local
    hooks:
      - id: ruff-format
        name: Ruff format staged Python
        entry: uv run --locked ruff format
        language: system
        types: [python]
      - id: ruff-lint
        name: Ruff lint staged Python
        entry: uv run --locked ruff check --fix
        language: system
        types: [python]
      - id: markdown
        name: Markdown hygiene and local links
        entry: uv run --locked python scripts/check_docs.py
        language: system
        types: [markdown]
      - id: dagger-affected
        name: Dagger affected checks
        entry: uv run --locked python scripts/ci_impact.py pre-push
        language: system
        stages: [pre-push]
        always_run: true
        pass_filenames: false
